#include <iostream>
#include <hiredis/hiredis.h>

#TODO
class redisCache
{
private:
    /* data */
public:
    redisCache(/* args */);
    ~redisCache();
    bool redisInit();
};

redisCache::redisCache(/* args */)
{
}

redisCache::~redisCache()
{
}
